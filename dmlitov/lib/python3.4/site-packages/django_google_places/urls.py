from django.conf.urls import *
from .views import PlaceDetailView

urlpatterns = patterns('',
    url(r'^place/(?P<pk>\d+)/$', 
        view=PlaceDetailView.as_view(),
        name="django_google_places.detail", 
    ),
)